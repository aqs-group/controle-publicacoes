rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All authenticated users can read/write for now (internal system)
    // TODO: Refine per role (admin vs. readonly)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
